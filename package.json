{
  "name": "playwright-pom",
  "version": "1.0.0",
  "description": "Playwright test project demonstrating the Page Object Model (POM) pattern for a sample TODO application.",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "typescript": "^5.2.0"
  }
}
