{
  "name": "playwright-pom",
  "version": "1.0.0",
  "description": "Projeto de testes Playwright utilizando o padrão Page Object Model (POM) para a aplicação TODO.",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "typescript": "^5.2.0"
  }
}
